{
  "name": "guess-game",
  "version": "1.0.0",
  "description": "Simple Guess Game, based on React",
  "main": "src/index.js",
  "scripts": {
    "babel": "babel client -d _tmp/babel",
    "babel-watch": "babel client -d _tmp/babel -w",
    "browserify": "browserify -e _tmp/babel/index.js -o build/index.js",
    "static": "node folders.js && cp -R static/*.* build/",
    "build": "npm run static && npm run babel && npm run browserify",
    "build-nb": "npm run static && npm run browserify && npm run server",
    "server": "node server.js"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/62mkv/guess-game.git"
  },
  "keywords": [
    "game",
    "react"
  ],
  "author": "62mkv",
  "license": "ISC",
  "dependencies": {
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-react": "^6.16.0",
    "hapi": "^15.2.0",
    "inert": "^4.0.2",
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-router": "^3.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "browserify": "^13.1.1",
    "shelljs": "^0.7.5"
  }
}
